1.async 是“异步”的简写，而 await 可以认为是 async wait 的简写。所以应该很好理解 async 用于申明一个 function 是异步的，而 await 用于等待一个异步方法执行完成。
还有一个语法规定，await 只能出现在 async 函数中，async 函数返回的是一个 Promise 对象。
所以说await 可以用于等待一个 async 函数的返回值。

不知道返回的promise对象是啥

2.看懂静态作用域概念   思考题代码不明白

3.静态作用域又叫做词法作用域，采用词法作用域的变量叫词法变量   采用动态作用域的变量叫做动态变量

this指向代码
