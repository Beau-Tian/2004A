什么是面向对象：
如果是面向过程的话比如把大象分三步放进冰箱那么他的每一个步骤都会用函数来实现，
需要依次调用，特别是每次如果变更的话都要把在系统重新读一遍，如果有函数那就调用
如果没有那就在定义一个，最后导致系统越来越杂乱无章难以管理。
而面向对象的话就是，比如给每个事物都定义一个对象，他的数据就是要执行的命令，他不会多做也不会少做，
描述一台电视，「数据」就是它的屏幕尺寸、亮度，「能力」就是播放《葫芦娃》。